# Copy gaps log

Comments stripped from `content/*.md` during Phase 1 wireframe generation, gathered here so they can be addressed before Phase 5.

Format: one row per gap. Resolve in Phase 4/5 by either supplying real content or confirming the placeholder is acceptable.

| Page | Section | Type | Note |
|---|---|---|---|
| home | What clients say (Testimonial33) | TODO | Testimonial copy not yet provided. Needs: 2–4 short quotes with name + role + company. Wireframe shows `<section data-status="awaiting-content">`. |
| home | Ready to move forward | NOTE | Final CTA section "Get Started / Speak with our team" exists in `content/home.md` but has no matching Relume component in `home/index.jsx`. Wireframe includes it flagged `data-status="extra-from-copy"`. Decide in Phase 2: add a Cta partial, or drop from copy. |

<!-- New entries appended as wireframes are built. Format above. -->
